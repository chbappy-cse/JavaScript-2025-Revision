/**
 * Join Method
 */

const names = ['Ashraf', 'Abid', 'Bappy', 'Sayem', 'Washi'];
console.log(names.join(''));
console.log(names.join(', '));
console.log(names.join(' + '));
console.log(names.join());
